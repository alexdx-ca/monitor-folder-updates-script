# monitor-folder-updates-script
 Script to make the updates on the server once you update/rename/delete any file in plugins folder on local machine. rclone and fswatch are required for script to work.
